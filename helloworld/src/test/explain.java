package test;
public class explain {
    /*
     * path是一个环境变量
     * 当在终端运行一个路径不完整的命令， 系统会在path中从上到下依次寻找exe文件
     * 
     * classpath是一个环境变量
     * 当在终端运行或编译一个java命令，系统会在classpath中从上到下依次寻找class或jar文件
     */
}
